import 'package:flutter/material.dart';

class Gepton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Developer Info'),
      ),
      body: Column(
        children: [Text('Coming Soon')],
      ),
    );
  }
}
